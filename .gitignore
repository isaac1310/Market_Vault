# Generated by build.mjs — never edit or commit these.
# Vercel runs the build itself, so the repo holds only the source.
deploy/
vault-mart.html

# Source material and archives — large, not needed to build or run
*.zip
*.docx

# Superseded copies, kept locally only
retired/

# Vercel CLI link data
.vercel/

# OS noise
.DS_Store
